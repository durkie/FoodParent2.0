import App from './app'

module Interfaces {
  export interface IAppProps {
    title : string;
  }
  export interface InterfaceComponentProps {
    router: App.Router;
  }
}

export default Interfaces;
